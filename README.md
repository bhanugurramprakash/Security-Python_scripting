# Security-Python_scripting

1. Build a log parser that reads a file and counts failed login attempts.
2. 2. Detect duplicate IP addresses from security log files.
3. Check whether URLs are secure using HTTPS validation.
